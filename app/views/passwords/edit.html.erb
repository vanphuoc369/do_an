<% provide(:title, "Thiết lập lại mật khẩu") %>
<h1>Reset password</h1>

<% provide(:title, "Cập nhật mật khẩu") %>

<div class="container">
  <div class="box-wrapper">
    <div class="box box-border">
      <div class="box-body">
        <h4>Thiết lập lại mật khẩu</h4>
        <%= form_for(@user, url: password_path(params[:id])) do |f| %>
          <hr>
          <%= render 'shared/error_messages' %>

          <%= hidden_field_tag :email, @user.email %>
          <div class="form-group">
            <%= f.label :password, "Mật khẩu mới" %>
            <%= f.text_field :password, class: "form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :password_confirmation, "Xác nhận mật khẩu" %>
            <%= f.password_field :password_confirmation, class: "form-control" %>
          </div>
          <div class="form-group text-right">
            <%= f.submit "Cập nhật mật khẩu", class: "btn btn-primary btn-block" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
